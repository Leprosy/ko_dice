- First of Last die will be the played die index or the state, absolute, array index?
- Do we keep the first 1x base mult and add to it?
- Check value before flip dice to set which axis will be used?
- Control.update_gui() params to tell which button is enabled/disabled?
